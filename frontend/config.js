// 前端运行配置。
//
// apiBase 决定前端请求后端 API 的地址：
//   - 本地开发（前端 5174 / 后端 8000 分离）：默认 "http://127.0.0.1:8000"；
//   - 生产（nginx 反代，前端与 API 同源）：改成空字符串 ""，走同源相对路径 /api/...。
//
// 浏览器右上角「API」框手动填写的值会存到 localStorage，优先于此文件。
window.APP_CONFIG = {
  apiBase: "http://127.0.0.1:8000",
};
